# 数据库设计说明（阶段二）

完整建表脚本见 `schema.sql`。表名使用复数蛇形；主键 `BIGINT UNSIGNED`；虚拟资产 `BIGINT`；概率为百万分比整数；配置表支持软删除，交易事实表不可删除。

关键索引包括：用户业务 ID 唯一、钱包用户唯一、资产流水用户时间索引、交易 request_id 幂等唯一、用户活动轮次唯一、宝箱节点唯一、阶段奖励领取唯一、榜单分数与到达时间联合索引、刷新令牌哈希唯一、后台日志管理员时间索引。
